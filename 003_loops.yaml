- name: Installing packages through loops
  hosts: localhost
  tasks:
  - name: Installing 
    ansible.builtin.debug:
      msg: "Installing pkg {{item}}"
      name: "{{item}}"
      state: prasent
    loop:
    - git
    - wget
    - mysql
    - postfix

